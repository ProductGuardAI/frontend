import{Search,ArrowRight,Plus}from'lucide-react';import{Link}from'react-router-dom';import{api}from'../api';import{Badge,ErrorBox,human,Spinner}from'../components';import{useLoad}from'../hooks';import type{Product}from'../types';import{useState}from'react';
export function Products(){const q=useLoad(()=>api<Product[]>('/products'));const[search,setSearch]=useState('');const[status,setStatus]=useState('');if(q.loading)return <Spinner/>;if(q.error||!q.data)return <ErrorBox message={q.error} retry={q.reload}/>;const list=q.data.filter(p=>`${p.name} ${p.brand}`.toLowerCase().includes(search.toLowerCase())&&(!status||p.status===status));return <><div className="page-head"><div><p className="eyebrow">CASE MANAGEMENT</p><h1>Product submissions</h1><p>Review and manage every supplier onboarding case.</p></div><Link className="button" to="/products/new"><Plus size={18}/>New submission</Link></div><section className="card"><div className="filters"><label className="search"><Search/><input placeholder="Search products or brands" value={search} onChange={e=>setSearch(e.target.value)}/></label><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">All statuses</option>{[...new Set(q.data.map(p=>p.status))].map(s=><option key={s} value={s}>{human(s)}</option>)}</select></div><div className="table-wrap"><table><thead><tr><th>Product</th><th>Supplier</th><th>Market</th><th>Status</th><th>Completeness</th><th>Risk</th><th>Updated</th><th/></tr></thead><tbody>{list.map(p=><tr key={p.id}><td><strong>{p.name}</strong><small className="block">{p.brand} · {p.category}</small></td><td>{p.supplier?.name}</td><td>{p.targetMarket}</td><td><Badge>{human(p.status)}</Badge></td><td>{p.completenessScore}%</td><td><b className={p.riskScore>50?'danger':''}>{p.riskScore}</b></td><td>{new Date(p.updatedAt).toLocaleDateString()}</td><td><Link to={`/products/${p.id}`}><ArrowRight size={18}/></Link></td></tr>)}</tbody></table></div></section></>}
