import{ArrowRight,ClipboardCheck,Clock3,ShieldAlert,PackageCheck,PackageX,Layers3,Activity}from'lucide-react';import{Link}from'react-router-dom';import{api}from'../api';import{Badge,ErrorBox,human,Spinner}from'../components';import{useLoad}from'../hooks';import type{DashboardData,Product}from'../types';
export function Dashboard(){const d=useLoad(()=>Promise.all([api<DashboardData>('/dashboard'),api<Product[]>('/products')]));if(d.loading)return <Spinner/>;if(d.error||!d.data)return <ErrorBox message={d.error} retry={d.reload}/>;const[m,products]=d.data;const stats=[[Layers3,'Total submissions',m.total,'blue'],[Clock3,'Awaiting AI review',m.awaitingAi,'amber'],[PackageCheck,'Ready for approval',m.ready,'green'],[ClipboardCheck,'Supplier action',m.supplierAction,'amber'],[ShieldAlert,'Compliance review',m.complianceReview,'red'],[PackageX,'Rejected',m.rejected,'slate']];return <><div className="page-head"><div><p className="eyebrow">PRODUCT ONBOARDING CONTROL CENTRE</p><h1>Good afternoon, Demo Reviewer</h1><p>Monitor submissions, resolve compliance issues, and keep products moving.</p></div><Link className="button" to="/products/new">New submission <ArrowRight size={17}/></Link></div><section className="stats">{stats.map(([I,l,v,t]:any)=><div className="stat" key={l}><span className={`stat-icon ${t}`}><I/></span><div><strong>{v}</strong><small>{l}</small></div></div>)}</section><div className="grid-main"><section className="card"><div className="card-head"><div><h2>Product pipeline</h2><p>Recently updated onboarding cases</p></div><Link to="/products">View all</Link></div><div className="table-wrap"><table><thead><tr><th>Product</th><th>Status</th><th>Completeness</th><th>Risk</th><th>Readiness</th><th/></tr></thead><tbody>{products.map(p=><tr key={p.id}><td><Link className="product-link" to={`/products/${p.id}`}><span>{p.name.slice(0,2).toUpperCase()}</span><div><strong>{p.name}</strong><small>{p.brand} · {p.category}</small></div></Link></td><td><Badge>{human(p.status)}</Badge></td><td><b>{p.completenessScore}%</b></td><td><b className={p.riskScore>50?'danger':''}>{p.riskScore}</b></td><td><div className="bar"><i style={{width:`${p.readinessScore}%`}}/></div><small>{p.readinessScore}%</small></td><td><Link to={`/products/${p.id}`}><ArrowRight size={17}/></Link></td></tr>)}</tbody></table></div></section><aside className="card activity"><div className="card-head"><div><h2>Recent activity</h2><p>Audit trail</p></div><Activity/></div>{m.recentActivity.map((a:any)=><div className="activity-item" key={a.id}><i/><div><strong>{human(a.action)}</strong><p>{a.notes}</p><small>{new Date(a.createdAt).toLocaleString()}</small></div></div>)}</aside></div></>}
