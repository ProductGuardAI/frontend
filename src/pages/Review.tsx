import{useState}from'react';import{useParams}from'react-router-dom';import{AlertTriangle,CheckCircle2,FileText,ExternalLink,Edit3,Check,MessageSquare,ShieldAlert,Download,Info,Clock3}from'lucide-react';import{api,patch,post}from'../api';import{Badge,Empty,ErrorBox,human,Score,Spinner}from'../components';import{useLoad}from'../hooks';import type{Product}from'../types';
const tabs=['Overview','Product data','Documents','Requirements','Claims','Compliance findings','Activity'];
export function Review(){const{id}=useParams();const q=useLoad(()=>api<Product>(`/products/${id}`),[id]);const[tab,setTab]=useState('Overview');const[notice,setNotice]=useState('');const[modal,setModal]=useState('');const act=async(action:string,notes='')=>{await post(`/products/${id}/review`,{action,reviewerName:'Demo Reviewer',notes});setNotice(`${human(action)} recorded`);setModal('');q.reload()};if(q.loading)return <Spinner/>;if(q.error||!q.data)return <ErrorBox message={q.error} retry={q.reload}/>;const p=q.data;return <>{notice&&<div className="toast"><CheckCircle2/>{notice}</div>}<div className="review-head"><div className="product-avatar">{p.name.slice(0,2).toUpperCase()}</div><div className="review-title"><div><Badge>{human(p.status)}</Badge><span>Updated {new Date(p.updatedAt).toLocaleString()}</span></div><h1>{p.name}</h1><p>{p.brand} · {p.supplier?.name} · {p.category} · {p.targetMarket}</p></div><div className="scores"><Score label="Completeness" value={p.completenessScore}/><Score label="Risk" value={p.riskScore} tone={p.riskScore>40?'red':'amber'}/><Score label="Readiness" value={p.readinessScore} tone="blue"/></div></div><div className="action-row"><button className="button green" onClick={()=>act('approve','Approved after human review')}><Check/>Approve</button><button className="button secondary" onClick={()=>setModal('request_supplier_information')}><MessageSquare/>Request information</button><button className="button secondary danger-text" onClick={()=>act('escalate_to_compliance','Escalated for specialist review')}><ShieldAlert/>Escalate</button><button className="button ghost" onClick={()=>setModal('reject')}>Reject</button></div><div className="tabs">{tabs.map(t=><button className={tab===t?'active':''} onClick={()=>setTab(t)} key={t}>{t}{t==='Compliance findings'&&<b>{p.findings?.filter(x=>x.status==='open').length}</b>}</button>)}</div><div className="tab-content">{tab==='Overview'&&<Overview p={p} onTab={setTab}/>} {tab==='Product data'&&<ProductData p={p} reload={q.reload}/>} {tab==='Documents'&&<Documents p={p}/>} {tab==='Requirements'&&<Requirements p={p}/>} {tab==='Claims'&&<Claims p={p}/>} {tab==='Compliance findings'&&<Findings p={p} reload={q.reload}/>} {tab==='Activity'&&<ActivityTab p={p}/>}</div>{modal&&<DecisionModal action={modal} p={p} close={()=>setModal('')} submit={act}/>}</>}
function Overview({p,onTab}:{p:Product;onTab:(s:string)=>void}){const missing=p.requirements?.filter(r=>r.mandatory&&r.status!=='present')??[];const risks=p.findings?.filter(f=>f.status==='open').slice(0,4)??[];return <div className="overview-grid"><section className="card span2"><div className="card-head"><div><p className="eyebrow">EXECUTIVE SUMMARY</p><h2>AI-assisted review summary</h2></div><Info/></div><p className="summary">{p.summary}</p><div className="recommend"><ShieldAlert/><div><strong>Recommended decision</strong><p>{human(p.status)}. Review every cited source before taking a final action.</p></div></div></section><section className="card"><div className="card-head"><h2>Product readiness</h2></div><div className="readiness-big"><strong>{p.readinessScore}</strong><span>/100</span></div><div className="big-bar"><i style={{width:`${p.readinessScore}%`}}/></div><p>{p.readinessScore>=80?'The case is close to a final decision.':'Corrections are required before approval.'}</p></section><section className="card span2"><div className="card-head"><div><h2>Key compliance risks</h2><p>Highest priority open findings</p></div><button onClick={()=>onTab('Compliance findings')}>View all</button></div>{risks.length?risks.map(f=><div className="risk-row" key={f.id}><Badge tone={f.severity}>{human(f.severity)}</Badge><div><strong>“{f.detectedText}”</strong><p>{f.explanation}</p><small><FileText/> {f.sourceLocation} · Confidence {Math.round(f.confidence*100)}%</small></div></div>):<Empty text="No open compliance findings"/>}</section><section className="card"><div className="card-head"><div><h2>Missing information</h2><p>{missing.length} items require attention</p></div></div>{missing.length?missing.map(r=><div className="missing" key={r.id}><AlertTriangle/><div><strong>{r.description}</strong><small>{human(r.requirementType)} · {human(r.status)}</small></div></div>):<Empty text="All mandatory items are present"/>}</section></div>}
function ProductData({p,reload}:{p:Product;reload:()=>void}){const[editing,setEditing]=useState('');const[value,setValue]=useState('');const save=async(id:string)=>{await patch(`/attributes/${id}`,{normalizedValue:value,reviewStatus:'reviewer_edited'});setEditing('');reload()};return <section className="card"><div className="card-head"><div><h2>Structured product information</h2><p>Every value retains its source and confidence.</p></div></div><div className="table-wrap"><table><thead><tr><th>Attribute</th><th>Raw value</th><th>Normalized value</th><th>Confidence</th><th>Source</th><th>Status</th><th/></tr></thead><tbody>{p.attributes?.map(a=><tr key={a.id}><td><strong>{human(a.attributeKey)}</strong></td><td>{a.rawValue}</td><td>{editing===a.id?<input autoFocus value={value} onChange={e=>setValue(e.target.value)}/>:<strong>{a.normalizedValue}</strong>}</td><td><span className="confidence">{Math.round(a.confidence*100)}%</span></td><td><small>{a.sourceLocation}</small></td><td><Badge>{human(a.reviewStatus)}</Badge></td><td><div className="row-actions">{editing===a.id?<button onClick={()=>save(a.id)}><Check/></button>:<button onClick={()=>{setEditing(a.id);setValue(a.normalizedValue)}}><Edit3/></button>}<button title="Verify" onClick={async()=>{await post(`/attributes/${a.id}/verify`);reload()}}><CheckCircle2/></button></div></td></tr>)}</tbody></table></div></section>}
function Documents({p}:{p:Product}){return <section className="card"><div className="card-head"><div><h2>Document register</h2><p>{p.documents?.length} files attached to this submission</p></div></div><div className="document-grid">{p.documents?.map(d=><article className="document" key={d.id}><span><FileText/></span><div><strong>{d.fileName}</strong><p>{human(d.documentType)}</p><small>English · {Math.round(d.classificationConfidence*100)}% confidence</small></div><Badge>{human(d.processingStatus)}</Badge><a href={`http://localhost:4000${d.fileUrl}`} target="_blank" rel="noreferrer"><ExternalLink/></a></article>)}</div></section>}
function Requirements({p}:{p:Product}){return <section className="card"><div className="card-head"><div><h2>Category requirements</h2><p>{p.category} · {p.targetMarket}</p></div></div>{p.requirements?.map(r=><div className="requirement" key={r.id}>{r.status==='present'?<CheckCircle2 className="ok"/>:<AlertTriangle className="warn"/>}<div><strong>{r.description}</strong><small>{human(r.requirementType)} · {r.mandatory?'Mandatory':'Optional'}{r.source&&` · ${r.source}`}</small></div><Badge tone={r.status}>{human(r.status)}</Badge></div>)}</section>}
function Claims({p}:{p:Product}){return <section className="card"><div className="card-head"><div><h2>Extracted marketing and packaging claims</h2><p>Evidence status is matched against submitted documents.</p></div></div>{p.claims?.length?p.claims.map(c=><div className="claim" key={c.id}><div><strong>“{c.claimText}”</strong><small>{human(c.claimType)} · Source: {c.sourceLocation} · {Math.round(c.confidence*100)}% confidence</small></div><Badge tone={c.riskLevel}>{human(c.riskLevel)} risk</Badge><Badge tone={c.evidenceStatus}>Evidence {human(c.evidenceStatus)}</Badge></div>):<Empty text="No claims were extracted"/>}</section>}
function Findings({p,reload}:{p:Product;reload:()=>void}){const resolve=async(id:string,kind:string)=>{const note=window.prompt(`Reviewer note for ${kind}`,'Reviewed against submitted evidence')??'';if(note||window.confirm(`Continue without a note?`)){await post(`/findings/${id}/${kind}`,{note});reload()}};return <div className="findings">{p.findings?.length?p.findings.map(f=><article className={`finding ${f.severity}`} key={f.id}><div className="finding-head"><Badge tone={f.severity}>{human(f.severity)}</Badge><Badge>{human(f.status)}</Badge><span>{Math.round(f.confidence*100)}% confidence</span></div><h2>“{f.detectedText}”</h2><p>{f.explanation}</p><div className="finding-meta"><div><small>SOURCE</small><strong><FileText/> {f.sourceLocation}</strong></div><div><small>EVIDENCE REQUIRED</small><strong>{f.requiredEvidence}</strong></div><div><small>EVIDENCE STATUS</small><Badge tone={f.evidenceStatus}>{human(f.evidenceStatus)}</Badge></div></div><div className="replacement"><small>SUGGESTED COMPLIANT WORDING</small><p>“{f.suggestedReplacement}”</p></div><div className="finding-actions"><button className="button secondary" onClick={()=>resolve(f.id,'resolve')}><Check/>Resolve</button><button className="button ghost" onClick={()=>resolve(f.id,'override')}>Override</button><button className="button ghost" onClick={async()=>{const reviewerNote=window.prompt('Internal note',f.reviewerNote??'');if(reviewerNote!==null){await patch(`/findings/${f.id}`,{reviewerNote});reload()}}}><MessageSquare/>Add note</button></div>{f.reviewerNote&&<p className="review-note"><strong>Reviewer note:</strong> {f.reviewerNote}</p>}</article>):<Empty text="No compliance findings"/>}</div>}
function ActivityTab({p}:{p:Product}){return <section className="card timeline"><div className="card-head"><div><h2>Activity history</h2><p>Immutable case events and reviewer actions</p></div><Download/></div>{p.activity?.map(a=><div className="timeline-item" key={a.id}><span><Clock3/></span><div><strong>{human(a.action)}</strong><p>{a.notes}</p><small>{a.reviewerName} · {new Date(a.createdAt).toLocaleString()}</small></div></div>)}</section>}
function DecisionModal({action,p,close,submit}:{action:string;p:Product;close:()=>void;submit:(a:string,n:string)=>void}){const[note,setNote]=useState('');const[generated,setGenerated]=useState('');const generate=async()=>{const v=await post(`/products/${p.id}/clarification`) as any;setGenerated(v.message);setNote(v.message)};return <div className="modal-back"><div className="modal"><h2>{human(action)}</h2><p>{action==='request_supplier_information'?'Generate or edit a structured clarification request.':'Document the reason for this reviewer decision.'}</p>{action==='request_supplier_information'&&<button className="button secondary" onClick={generate}>Generate from findings</button>}<textarea rows={generated?12:6} value={note} onChange={e=>setNote(e.target.value)} placeholder="Add reviewer notes…"/><div><button className="button ghost" onClick={close}>Cancel</button><button className="button" onClick={()=>submit(action,note)}>Record decision</button></div></div></div>}
